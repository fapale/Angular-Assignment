import { SearchCardsPipe } from './search-cards.pipe';

describe('SearchCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
